스테이지 데이터 작성

스테이지 Dto
스테이지 기획 데이터상 인덱스도 받고 플레이어 마지막 위치도 받아야 됨.
그런데 Vector3 이런건 유니티 엔진의 구조체라서 서버에 없음.
그래서 float 배열이나 변수 xyz 선언해서 받음.

스테이지 Bo를 만듦
이전 스테이지 인덱스도 받아야 함.
워프가 있다고 한다면 한 맵에 여러 포탈이 있을 수 있으니 이전 인덱스를 알아야 맵을 바꿨을 때 플레이어의 올바른 위치를 앎.

INetworkHandler 에서 GetStage 작성

스테이지 관련 핸들러 및 이런저런 필드 작성.

StageManager작업.
타이틀 컨트롤러에서 작업.

컴파일하고 빈 객체에 싱글톤 객체로 넣기

So에 오류메시지랑 코드 있는거 거슬려서
DtoBase에 [HideInInspector] 적용하기

적용하고 컴파일하고 실행


캐릭터 정의할 것. Actor 클래스 생성.

using ActorState = Define.Actor.ActorState;
using ActorAnim = Define.Actor.ActorAnim;

namespace 안에 이렇게 작성.

BoActor생성

    /// BO 는 통신으로 받은(섭) 데이터, 기획에서 정의한 데이터, 인게임에서만 사용하는 임시 데이터(휘발성)
    /// 세 종류의 데이터가 올 수 있음.

Actor 클래스에서 Collider도 받아둠. 이건 모든 콜라이더의 베이스

외부에서 데이터 받는게 굉장히 편함. 의존성 주입 검색해서 찾아보면 좋음.

Actor의 공통되는 처리 부분들 구현.

State Controll 부분 구현
그리고 AnimationEvent 구현
        // 애니메이션의 특정 프레임에 내가 호출하고자 하는 메서드를 등록하여 해당 프레임에 도달했을 때,
        // 등록한 메서드가 실행되도록 하는 기능 (등록할 수 있는 메서드는 접근제한자가 public인 메서드만)

이제 공격 구현할 것임.
공격은 따로 클래스 만들어서 할 것.

그런데 정석으로 