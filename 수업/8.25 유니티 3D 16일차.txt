풀에서 빼갈애 없을 때 오브젝트 풀에서 원래 타입 이름으로 가져오게 했을 것인데 몬스터는 이름이 여러개임.
그런데 몬스터는 타입이 하나임. 이게 문제가 되서 못가져옴.
나중에 각각의 몬스터별로 AI가 달라지게 된다면 몬스터에서 파생클래스 만들어서 상속받는 몬스터들 만들었을 때는 괜찮은데 이렇게 파생클래스까지 만들건 아니라서 몬스터 타입만 쓸건데 이럴 때 객체를 제대로 찾기 어령무.
조건에 맞는 객체로 바꾸기.


그리고 로딩 씬에 UI배치하기. 상호작용 할거 없으니 EventSystem 날림
그리고 안쪽에 빈객체 UILoading 만들고 Stretch 한다음에 이미지 넣고 채우기.
그리고 백그라운드 아래에 Image 다시 만들어서 LoadingProgressBar로 쓰기
그리고 위치설정하고 Outline 컴포넌트 넣기.
그리고 LoadGauge  Image로생성.
그리고 게이지에 fill 넣고 스트레치하게.

LoadStateDescription 텍메프로 생성.
그리고 폰트 메이플 넣기.

이제 UI 스크립트 만듦.
UILoading


다 구현하고 게임매니저 WaitForLoad() 안에서 하도록.

왠만해서 스크립트 우선순위 안정해주면 랜덤실행임.

타이틀 컨트롤러에서 타이틀 씬 지금 못보니까 로컬함수 만들어서 실행.

내가 굽지 않은 문자가 있거나 하면 TMP에서 다른 폰트 애셋으로 기본폰트 같은 걸로 만들어서 SubMesh 추려햏

UITitle 임시적으로 클래스 한 ㅏ만ㄷ름.
로딩바 애니메이션 처리.

TitleController에서 Coroutine 처리

Window 에서 AI 에 Navigation 있음.
여기에 영역 설정하고 구우면 됨. 프리팹에 구울 수는 없음. 유니티가 허용 안함.
구우면 이동 가능 영역이 파랑으로 나타남. 이 Nav Mesh Agent 달린 애들은 이 영역 돌아다니는게 가능.
여기에 장애물이 있으면 장애물 영역은 파랑 영역에서 제외됨. 자동으로 갈 수 있는 영역이 바뀌게 됨. 장애물 주변으로 좀 넓은 영역 통행 못하게 되어있는데 Agent 설정에서 이 영역 돌아다니는 Agent의 부피를 대충 설정 가능한데 이걸로 설정하고 장애물 설정은 Nav Mesh Obstacle을 붙여서 대략적으로 얼마나 사이즈 차지하는지 설정해두면 에이전트 값이랑 알아서 계산이 되어서 지도를 깔끔하게 구워내짐.
문제는 프리팹에서 못씀.

근데 사용할 수 있는 방법이 있음. 공식 Navigation에는 없음.
근데 유니티가 확장중인 기능들은 깃헙에 올려놓음.
unity navMeshGithub 검색하면  유니티 테크놀로지에 NavMeshComponents 라고 있는게 있음.
이게 NavMesh 확장해서 사용하는 기능.
다양한 기능 있음.

이 NavMesh가 런타임 말고 작업타임에 만들어놓은건데 런타임에 쓰려고 만든게 이 확장기능.
그래서 런타임에 네비 지도를 생성 가능함. 대신 런타임 쓸 때 당연히 작업비용이 큼.
그러니 일반적으로 못쓰게 막아둔거.

gizmo랑 NavMesh 폴더만 챙겨다가 Assets에 NavMeshExtension 폴더를 만듦.
이런 확장 기능은 원래 마우스로만 못하고 코드도 작성하고 해야되는게 많은데 프리팹에서 하는건 다행히 그냥 가능.

비기너 헌팅 그라운드에 NavMesh Surface 잇음. 이거 추가해줌. 레이어 플로어만 해주고. 그리고 구우면 네브 메쉬 데이터가 파일로 만들어져서 이 프리팹이 있는 곳에 위치하게 됨.
Stage에 생김. 이 데이터가 여기에 있는 것.

이제 몬스터에서 움직임 관련 메소드 작성.